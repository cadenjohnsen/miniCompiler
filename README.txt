This program tests a miniature version of the compiler. It will run with different user input commands and reject commands that do not work properly.

To Compile and Run:
gcc -o run miniCompiler.c
run
